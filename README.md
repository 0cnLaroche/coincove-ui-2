# Ecove E-Commerce platform

Configuration should defined in json in the src/config directory. Configs are read (and overriden) in this order 
1. default 
2. local

To start dev server
```npm start```

To build 
```npm run-script build```
