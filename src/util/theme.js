/* These styles are applied to the entire app as theme */
const styles = {
    palette: {
        primary: {main:  process.env.REACT_APP_THEME_PRIMARY},
        secondary: {main: process.env.REACT_APP_THEME_SECONDARY}
    }
}

export default styles;