import React, { useState } from 'react';
import { 
    Button, 
    TextField, 
    Dialog, 
    DialogActions, 
    DialogContent, 
    DialogContentText, 
    DialogTitle 
} from '@material-ui/core';

consst ShippingDialog = ({open}) => {

    const [open, setOpen] = useState(false);
    const handleClose = () => {
        setOpen(false);
    }

    return (

    )
}